<launch>

     <node name="rviz" pkg="rviz" type="rviz" />

     <node pkg="tf" type="static_transform_publisher" name="base_broadcaster" args="0 0 0 0 0 0 base_link world_frame 50" />

	<node name="visual_servo_control_node" pkg="pepper_servo_package" type="visual_servo_control_node" output="screen" respawn="true" >
	
	
	     <!-- General paths for halcon  -->
		<param name="halcon_script_path" 	               type="str"	value="$(find pepper_servo_package)/halcon_scripts/cake_test.hdev" />		
		<param name="halcon_external_procedures_path" 	type="str"	value="$(find pepper_servo_package)/halcon_external_procedures" />	
		<param name="camera_parameters_path" 	          type="str"	value="$(find pepper_servo_package)/camera_parameters/tis_camera_parameters.dat" />	
	
	
	     <!-- Specific names of defined functions -->
	     <param name="initialize_camera_function" 	     type="str"	value="initialize_camera_tis" />
	     <param name="grab_rectified_image_function" 	     type="str"	value="grab_rectified_image" />
	     <param name="find_points_function" 	          type="str"	value="find_points" />
	     <param name="display_grabbed_image" 	          type="str"	value="display_grabbed_image" />
	     <param name="close_frame_grabber_function" 	     type="str"	value="close_frame_grabber" />
	        
	     <param name="test" 	          type="str"	value="test" />   
	        
	</node>

</launch>


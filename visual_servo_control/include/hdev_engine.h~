#ifndef HDEV_ENGINE_H
#define HDEV_ENGINE_H

#include "ros/ros.h"
#include "std_msgs/String.h"
#include "HalconCpp.h"
#include "HDevEngineCpp.h"
#include <halcon/halcon_init.h>
#include <halcon/request_tuple.h>
#include "window_handling.h"

using namespace HalconCpp;
using namespace HDevEngineCpp;

HDevEngine hdevengine;
HDevProgram my_program;
HDevProgramCall program_results;
HTuple result;

std::string halcon_program_path;
std::string halcon_ext_proc_path;

bool initialized;
bool program_lock;

void initializeHalconScript();
void triggerCallback(const std_msgs::String::ConstPtr& msg);
bool requestOutput(halcon::request_tuple::Request  &req, halcon::request_tuple::Response &res);
int main(int argc, char **argv);

#endif

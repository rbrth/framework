#include <ros/ros.h>
#include <crops_msgs/moveArmAction.h>
#include <crops_state_machine/SimpleActionState.h>

#ifndef MOVETOHOMESTATE_H_
#define MOVETOHOMESTATE_H_

class MoveToHomeStateSim : public SimpleActionState<crops_msgs::moveArmAction, crops_msgs::moveArmGoal>
{
private:
  // Define the 'home' position
  static const double home_x_= 0.2, home_y_ = 0.2, home_z_ = 0.2;
public:
  MoveToHomeStateSim();
  virtual ~MoveToHomeStateSim();
  void homeFeedbackCb(const crops_msgs::moveArmFeedbackConstPtr feedback);
  std::string execute(std::map<std::string, boost::any> * data);
};

#endif /* MOVETOHOMESTATE_H_ */

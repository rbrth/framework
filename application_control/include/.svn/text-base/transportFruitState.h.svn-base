#include <ros/ros.h>
//#include <crops_msgs/moveRealArmAction.h>
#include <crops_msgs/p2pAction.h>
#include <crops_state_machine/SimpleActionState.h>
#include <crops_msgs/manipulatorState.h>

#ifndef TRANSPORTFRUITSTATE_H_
#define TRANSPORTFRUITSTATE_H_

class TransportFruitState : public SimpleActionState<crops_msgs::p2pAction, crops_msgs::p2pGoal>
{
private:
  crops_msgs::Statistics stat_msg_;
  ros::NodeHandle n;
  ros::Publisher PubManipulatorState;
  bool xpc_target_; //xpc target flag
  XmlRpc::XmlRpcValue bin_location_;
public:
  TransportFruitState();
  virtual ~TransportFruitState();
  void feedbackCb(const crops_msgs::p2pFeedbackConstPtr feedback);
  std::string execute(std::map<std::string, boost::any> * data);
};

#endif /* TRANSPORTFRUITSTATE_H_ */
